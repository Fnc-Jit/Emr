
  # Health ID App UI (Copy)

  This is a code bundle for Health ID App UI (Copy). The original project is available at https://www.figma.com/design/CTryqpaEB1G6vmSw3tU7JA/Health-ID-App-UI--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  